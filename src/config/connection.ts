interface DBConfig {
  host: string;
  user: string;
  password: string;
  database: string;
}

const dbConfig: DBConfig = {
  host: "localhost",
  user: "strongdev",
  password: "strongDev221024!",
  database: "shelter",
};

export default dbConfig;
